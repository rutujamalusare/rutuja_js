let obj = {
    nm : 'Android',
    age : 10,
    calcVer : function() {
        return 10
    }
}

console.log(`
    Name is ${obj.nm} 
    age is ${obj.age} 
    version is ${obj.calcVer()}
`)